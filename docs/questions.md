调整imu，写一个测试，发送几组imu数据，使得机器对齐，或者手动摆正，最后以步兵为准

弹速修改，同时应该动态添加能调试的偏移，看看能不能拟合，比如(1m,pitch:+1),(2m,pitch:+2)，现在的，找一个物理模型，纯理想物理计算不行

调坏standard3的
R_camera2gimbal: [-0.027182119030230909, -0.12616154330853446, 0.99163723074269183, -0.99949106557517331, 0.019998323121329122, -0.024853106601381177, -0.016695575474690555, -0.99180811252093692, -0.12664093215554434]
t_camera2gimbal: [0.13160669975045827, 0.10377721766577375, 0.024908271912914642]

R_gimbal2imubody: [0, -1, 0, 1, 0, 0, 0, 0, 1]应该是一样的吗？

standard3的其他参数是什么

现在有回退传统视觉方法的功能吗

现在代码除掉抽离出来的ros2自启服务接口，应该订阅了 gimbalToVision 拿到c板数据，同时自启一个publisher topic visionToGimbal 发送自瞄指令。然后自启ros2服务接口做的是读串口，然后启动一个publisher叫gimbalToVision 然后再去订阅 visionToGimbal 这个topic，收到消息就写串口

然后由于串口通信只能单个程序打开，我的ros2包导航应该把原来的阅读串口功能做分流，如果是5A开头的自瞄包，就发到publisher叫gimbalToVision，如果是原来的导航包就发到原来导航的publisher，然后再去订阅 visionToGimbal 这个topic，收到消息就写串口，也订阅导航的发包topic，有消息就写，这里写串口会有冲突吗
现在的[text](../references/gimbal_ros2_bridge_minimal)是完整实现吗，是不是能直接给导航的负责人，信息都全了吗


